import 'package:flutter/material.dart';
import 'package:instagram_ui/util/ExploreGrid.dart';
class AccountReel extends StatelessWidget {
  const AccountReel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      body: ExploreGrid(),
    );
  }
}